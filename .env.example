# Optional. Without a key pastewise uses a built-in heuristic classifier.
# Get one at https://console.typesafe.ai/keys. Read on the server only.
TYPESAFE_API_KEY=

# Pin a model version so classifications don't shift under you.
JEV_MODEL=jev-latest
